"""HTML to Markdown converter with source map for provenance. Stub."""

from __future__ import annotations


def html_to_markdown(html: str, source_filing: str = "") -> tuple[str, dict]:
    """Convert HTML filing to markdown with source_map. Stub."""
    return "", {}
